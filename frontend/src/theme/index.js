import {createTheme} from '@material-ui/core'

export default createTheme({
    palette: {
        backgroud: {
            default: '#F1F3F4',
        },
        primary: {
            main: '#14194C'
        }
    },
    typography: {
        fontFamily: 'Mulish, sans-serif',
        h4: {
            fontWeight: 800
        }
    }
})
